apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-svc

spec:
  replicas: 1

  selector:
    matchLabels:
      app: schedule-svc

  template:
    metadata:
      labels:
        app: schedule-svc

    spec:
      containers:
        - name: schedule-svc
          image: eonmsa/schedule-svc:v2
          imagePullPolicy: Always

          ports:
            - containerPort: 8082

          envFrom:
            - configMapRef:
                name: schedule-config
            - secretRef:
                name: schedule-secret